#ifndef __EMBEDDED__
#define __EMBEDDED__

@disable stdlibs

#include <reg51.h>

#define SetPort(p, s)\
	p = s

void delay() {
	int a,b;
	
	for(a=0;a<100;a++)
		for(b=0;b<1000;b++);
}

#endif //stack embed